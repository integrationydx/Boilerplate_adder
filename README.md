# Language Boilerplate Injector

Detects the active document language and nearby project signals, then inserts boilerplate for:

- Competitive programming
- Web development
- SQL queries
- React
- Node.js

## Usage

Run the `Inject Boilerplate` command from the Command Palette in an open editor.
